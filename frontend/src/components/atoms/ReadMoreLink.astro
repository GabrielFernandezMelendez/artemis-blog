---
import Text from "./Text.astro";

interface Props {
  label?: string;
}

const { label = "Read story →" } = Astro.props;
---

<Text as="span" variant="caption" class="text-warm hidden sm:inline mt-1"
  >{label}</Text
>
